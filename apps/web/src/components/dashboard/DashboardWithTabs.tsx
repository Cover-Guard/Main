'use client'

/**
 * DashboardWithTabs — simplified wrapper (compare tab removed per redesign).
 * Compare functionality is now accessible via buttons on individual property cards.
 */
export function DashboardWithTabs({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>
}
