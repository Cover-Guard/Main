# Daily smoke + QA run — 2026-05-13

Scheduled task: `daily-smokeqa-testing`. Today's run had no bug fixes —
the `scripts/qa/smoke-test.ts` file started clean for the **FIFTH
consecutive day** (1652 lines from 2026-05-12's state, 92 probes
intact, no truncation marker, no orphan fragments, brace balance
472/472). The recurring Edit-tool corruption that bit the file FIVE
times in 8 days did NOT recur today. The whole run was coverage
expansion: a 2-probe pass that grows the **per-router CORS pin matrix
from 7-of-9 to 8-of-9 routers** (`OPTIONS /api/stripe/subscription`,
eighth distinct router file) and **extends the response-shape pinning
axis from 2-of-8 to 3-of-8 endpoints** (`/:id/risk` exact top-level
cardinality).

## What ran

| Suite | Status | Notes |
|---|---|---|
| `npx tsx scripts/qa/smoke-test.ts` | **fail-to-fetch** | Same sandbox constraint as 2026-05-02 through 2026-05-12 — no live API on :4000. Plus today, `npx` itself returns `ENOSPC` again (sandbox `/sessions` is at 100% disk usage — **tenth day in a row**). `node_modules/.bin/tsx` and `node_modules/.bin/jest` are both still absent. Static structural review only. |
| Static structural review of `smoke-test.ts` | **passed (no fix needed)** | Initial state on disk: 1652 lines (post-2026-05-12 state), ends with `})` after `run().catch(...)` block, 92 `await runTest(` invocations, no `carriers shou$` truncation marker, no `verage` orphan at column 1, balanced braces (472 open / 472 close). The file was preserved cleanly from 2026-05-12. |
| `npx tsx scripts/qa/health-check.ts` | not executed | no live API in sandbox |
| `npx tsx scripts/qa/env-validate.ts` | not executed | no `.env` in sandbox |
| `apps/api` jest suite | not executed | `node_modules/.bin/jest` shim missing in mount; `npx jest` blocked by `ENOSPC`; same constraint as prior runs. |

## Bugs fixed (0)

The recurring Edit-tool corruption did **not** recur today. Five
consecutive clean days (2026-05-09, 2026-05-10, 2026-05-11, 2026-05-12,
2026-05-13). The mitigation established 2026-05-09 (single atomic
Python rewrite from the bash sandbox) was reused today and again worked
cleanly. Today's edits were applied via `python3 /tmp/patch_smoke_05_13.py`
— three sequential `str.replace` operations (header changelog + OPTIONS
stripe probe + /risk response-shape probe) followed by one atomic
`PATH.write_text(...)`. Pre- and post-write integrity checks (line
count, probe count, brace balance, top-level `run().catch(` count,
truncation-marker absence, orphan-fragment absence, file ends with
`})`) all passed.

This sidesteps the recurring `~30 KB sequential-Edit truncation` bug
entirely, and remains the documented workflow for any modification to
`smoke-test.ts` going forward.

Static review of all nine routers (advisor, alerts, auth, clients,
dashboard, deals, notifications, properties, stripe) found no missing
`requireAuth` and no mismatched verb pairs. CORS configuration in
`apps/api/src/index.ts` remains correct (cors mounted line 104,
`app.options('*', cors())` mounted line 105, both before any router).
The cached-endpoint Cache-Control matrix is unchanged at **7 of 8
endpoints** (only `/:id/report.pdf` remains, and it's auth-gated). The
refresh=true matrix remains **CLOSED at 6-of-6**.

## Coverage gaps closed (2 new probes + tightened file-integrity guard)

`Main/scripts/qa/smoke-test.ts` (1777 lines, 94 probes, +2 from
yesterday):

1. **`OPTIONS /api/stripe/subscription` from allowed origin → 204,
   ACAO echoed (and not `*`)** — eighth mounted surface in the
   per-router CORS pin matrix, beyond `/api/properties/search`
   (2026-05-06), `/api/auth/me` (2026-05-07), `/api/clients`
   (2026-05-08), `/api/dashboard` (2026-05-09), `/api/advisor/chat`
   (2026-05-10), `/api/deals` (2026-05-11), and `/api/alerts`
   (2026-05-12). `stripe.ts` is yet another router file
   (`stripeRouter` for `/subscription`, `/checkout`, `/portal` — all
   `requireAuth`-gated per-handler — plus `stripeWebhookRouter` for
   the raw-body `/webhook` flow). The OPTIONS preflight goes through
   `cors()` and `app.options('*', cors())` in `index.ts` BEFORE the
   per-handler `requireAuth` ever fires. A per-router CORS override on
   the stripe router would let any origin issue credentialed GETs to
   `/api/stripe/subscription` — leaking the victim user subscription
   plan, status, current_period_end, and Stripe customer/subscription
   IDs to an attacker-controlled origin via a XHR initiated from the
   victim browser session. **Per-router pin matrix is now 8-of-9
   routers.**

2. **`GET /api/properties/:id/risk` response shape: exactly 2 top-level
   keys** — third member on the response-shape pinning axis. The
   existing `/:id/risk` probe (added 2026-05-07) already asserts that
   the documented inner fields (`overallRiskScore`, `overallRiskLevel`,
   `flood`, `fire`, `wind`, `earthquake`, `crime`) are present; today's
   probe additionally asserts that the top-level body has EXACTLY 2
   keys (`{ success, data }`) — no extras. Like the 2026-05-12 `/:id`
   pin, only top-level cardinality is pinned; the inner data shape on
   `/risk` is wide (each hazard sub-object has its own nested fields
   that have evolved with the risk-service refactor history) and
   pinning the inner key set would churn on legitimate risk-service
   evolution. The regression class this probe catches is a future PR
   that adds a NEW top-level body key (e.g. `{ success, data, debug }`
   — a debug or admin field on the public path). **Response-shape pin
   matrix is now 3-of-8 endpoints.**

`Main/apps/api/src/__tests__/daily-review-2026-05-13.test.ts` (589
lines, 7 describe blocks, 43 it blocks):

3. **Smoke-test surface contract (2026-05-13)** — pins the 94-entry
   `REQUIRED_SMOKE_PROBES_05_13` array (≥94 probes, strictly grows
   over the 92 of 2026-05-12, every entry uniquely worded, today's
   2 additions are each individually asserted). Reverse-pin: every
   `refresh=true` probe (still 6) must NOT assert `s-maxage=`.
4. **Smoke-test file integrity** — carries forward the 2026-05-12
   widened guard. Tightens line-count floor from **1600 → 1700**.
   Carries forward the `carriers shou$` truncation-marker check and
   the `verage` orphan-fragment check. Adds an extra cross-check: at
   least 94 `runTest(` invocations on disk (must match the contract
   list above). Retains the balanced-braces structural-corruption
   guard and the "exactly one top-level `run().catch(`" guard.
5. **OPTIONS /api/stripe/subscription preflight** — documents the
   per-router pin matrix invariant (8 of 9 routers) and asserts 9
   OPTIONS-prefixed probes total (search disallowed + search allowed +
   auth/me + clients + dashboard + advisor + deals + alerts +
   stripe/subscription). Documents the one remaining router target
   (notifications) as a carryover — pinning it as a *negative*
   assertion so a future PR adding the probe will need to update the
   expectation.
6. **Property-id-bound Cache-Control contracts** — documents the
   8-endpoint Cache-Control matrix (7 cached pinned, 1 unpinned —
   `report.pdf` is auth-gated). Documents the 6-endpoint refresh=true
   matrix (6 pinned — **CLOSED**, carried). Asserts ≥7 `s-maxage=N`
   cache pins and exactly 6 refresh=true no-cache pins.
7. **Verb-parity audit** — carryover from 2026-05-03. No new mutating
   verbs today (today's additions are an OPTIONS preflight + a GET
   response-shape probe). Asserts that every PATCH/DELETE probe in
   the surface contract ends with `→ 401`.
8. **CORS configuration sanity** — documents the per-router pin
   matrix (8-of-9 routers). Asserts ≥9 OPTIONS-prefixed probes and
   confirms each of the eight pinned surfaces appears.
9. **Response-shape pinning axis** — asserts exactly 3 response-shape
   probes today (`/report`, `/:id`, `/:id/risk`), documents the 5
   endpoints that remain unpinned on this axis (insurance,
   insurability, carriers, walkscore, public-data) as negative pins
   so a future PR adding response-shape probes for them will need to
   update the expectation. Adds the new axis-growth invariant (probe
   count is strictly monotonic across days: 1 → 2 → 3). Asserts the
   `/:id/risk` probe text references only "2 top-level keys" without
   a "data keys" clause so a future cleanup that adds inner-shape
   assertion also updates the contract.

## Files touched

- `Main/scripts/qa/smoke-test.ts` — applied 2 new probes via a single
  atomic Python rewrite (run from `/tmp/patch_smoke_05_13.py` /
  `outputs/patch_smoke_05_13.py`). Grew from 1652 lines / 92 probes /
  ~86 KB to 1777 lines / 94 probes / ~92 KB. Added the 2026-05-13
  changelog block at the top of the file-header docblock, the
  `OPTIONS /api/stripe/subscription` always-on probe after the
  `OPTIONS /api/alerts` probe block, and the `/:id/risk` response-
  shape probe after the existing `/:id/risk Cache-Control` probe.
  Brace balance moved cleanly from 472/472 to 488/488. The cached-
  endpoint matrix is unchanged at 7 of 8 endpoints (only `report.pdf`
  remains, auth-gated). The refresh=true matrix is unchanged at
  CLOSED 6 of 6.
- `Main/apps/api/src/__tests__/daily-review-2026-05-13.test.ts` (new)
  — pinned probe list (94 entries), tightened file-integrity guard
  (line-count floor 1600 → 1700, today's runTest cross-check at ≥94,
  brace-balance guard, exactly-one-`run().catch(` guard, carryover
  truncation-marker check, carryover orphan-fragment check), per-
  router CORS pin matrix doc (8-of-9), cached-endpoint Cache-Control
  matrix doc, refresh-branch matrix doc (6-of-6 closed, carried),
  response-shape pin matrix doc (3-of-8), axis-growth invariant.
- `Main/daily-qa-2026-05-13.md` (new) — this report.
- `outputs/patch_smoke_05_13.py` — atomic Python rewrite script
  (mitigation-compliant).
- `outputs/PR_DESCRIPTION-2026-05-13.md` — body for the GitHub PR.

## Probe count over time

| Date | Probes | Delta | Notes |
|---|---|---|---|
| 2026-04-30 | ~28 | baseline | search → 401, /health, /robots.txt, suggest, ticker, deals, alerts, vapid |
| 2026-05-01 morning | ~32 | +4 | analytics → 404, advisor 401, geocode 400, walkscore 404 |
| 2026-05-01 evening | ~46 | +14 | full unauth-401 set, register 400, totally-fake 404, carriers happy-path |
| 2026-05-02 | ~47 | +14 (write-side + property-id-bound) | PATCH/DELETE /me, POST clients/deals, ack alert, walkscore 200, public-data, report.pdf 401, checklists, save, quote-request |
| 2026-05-03 | ~53 | +9 | PATCH/DELETE clients/deals, bare detail 404, webhook 400, save DELETE, checklists PATCH/DELETE |
| 2026-05-04 | 56 | +3 | unauth /report 404, /report.pdf 404 (NOT 401), /report 200 |
| 2026-05-05 | 61 | +5 | HEAD /health, GET /properties 404, GET webhook 404, POST search 404, HEAD /report.pdf 401 |
| 2026-05-06 | 68 | +7 | OPTIONS preflight × 2, geocode boundaries × 2, HEAD bogus /report.pdf 404 |
| 2026-05-07 | 74 | +6 | suggest CC, suggest standard rate-limit headers, OPTIONS /api/auth/me, /:id CC, /:id/risk CC |
| 2026-05-08 | 80 | +6 | OPTIONS /api/clients, walkscore CC, insurance CC, insurability CC, carriers CC (1h NOT 2h), risk?refresh=true no-cache |
| 2026-05-09 | 86 | +6 | OPTIONS /api/dashboard, public-data CC, insurance?refresh=true, carriers?refresh=true, insurability?refresh=true, walkscore?refresh=true |
| 2026-05-10 | 88 | +2 | OPTIONS /api/advisor/chat (5th-router CORS pin), public-data?refresh=true (closes refresh matrix) |
| 2026-05-11 | 90 | +2 | OPTIONS /api/deals (6th-router CORS pin), /report response-shape (initiates response-shape axis) |
| 2026-05-12 | 92 | +2 | OPTIONS /api/alerts (7th-router CORS pin), /:id response-shape (axis at 2-of-8) |
| 2026-05-13 | 94 | +2 | OPTIONS /api/stripe/subscription (8th-router CORS pin), /:id/risk response-shape (axis at 3-of-8) |

## Cached-endpoint Cache-Control matrix (status as of 2026-05-13)

| Endpoint | TTL (s-maxage) | Cached pinned? | Refresh branch pinned? |
|---|---|---|---|
| `GET /api/properties/suggest` | 300 (5 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id` | 1800 (30 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id/risk` | 7200 (2 h) | ✅ since 2026-05-07 | ✅ since 2026-05-08 |
| `GET /api/properties/:id/insurance` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/insurability` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/carriers` | 3600 (1 h, NOT 2h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/walkscore` | 86400 (24 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/public-data` | 86400 (24 h) | ✅ since 2026-05-09 | ✅ since 2026-05-10 |
| `GET /api/properties/:id/report.pdf` | private, max-age=300 | ❌ requires auth-token strategy | n/a (auth-gated, never CDN-cached) |

Cached-side: **7 of 8 endpoints pinned** (unchanged). Refresh-side:
**6 of 6 endpoints pinned (CLOSED — carried)**. Total Cache-Control
matrix: **13 of 14 cells pinned (93%) — unchanged from 2026-05-12**.

## Per-router CORS pin matrix (status as of 2026-05-13)

| Router file | Mounted surface | Pinned by smoke probe? |
|---|---|---|
| `properties.ts` | `OPTIONS /api/properties/search` | ✅ since 2026-05-06 (allowed + denied) |
| `auth.ts` | `OPTIONS /api/auth/me` | ✅ since 2026-05-07 |
| `clients.ts` | `OPTIONS /api/clients` | ✅ since 2026-05-08 |
| `dashboard.ts` | `OPTIONS /api/dashboard/ticker` | ✅ since 2026-05-09 |
| `advisor.ts` | `OPTIONS /api/advisor/chat` | ✅ since 2026-05-10 |
| `deals.ts` | `OPTIONS /api/deals` | ✅ since 2026-05-11 |
| `alerts.ts` | `OPTIONS /api/alerts/carrier-exits` | ✅ since 2026-05-12 |
| `stripe.ts` | `OPTIONS /api/stripe/subscription` | ✅ since 2026-05-13 |
| `notifications.ts` | `OPTIONS /api/notifications/dispatch` | ❌ tomorrow's target — last 1 of 9 |

**8 of 9 routers pinned.** Each pin is on a different router file,
so a per-router CORS override on any single pinned router would fail
at least one probe. **Only `notifications.ts` remains unpinned** —
tomorrow closes the matrix at 9-of-9.

## Response-shape pin matrix (status as of 2026-05-13)

| Endpoint | Documented data keys | Response-shape pinned? |
|---|---|---|
| `GET /api/properties/:id/report` | property + risk + insurance + insurability + carriers + publicData (6) | ✅ since 2026-05-11 (2 top + 6 data) |
| `GET /api/properties/:id` | property | ✅ since 2026-05-12 (top-level only) |
| `GET /api/properties/:id/risk` | risk profile | ✅ since 2026-05-13 (top-level only) |
| `GET /api/properties/:id/insurance` | insurance estimate | ❌ tomorrow's target |
| `GET /api/properties/:id/insurability` | insurability status | ❌ |
| `GET /api/properties/:id/carriers` | carriers list (varying shape — array or { carriers: [] }) | ❌ |
| `GET /api/properties/:id/walkscore` | walk/transit/bike scores | ❌ |
| `GET /api/properties/:id/public-data` | images + tax + listings + amenities (aggregate) | ❌ |
| `GET /api/properties/suggest` | suggestions array | ❌ |

**3 of 9 endpoints pinned.** The 6 remaining endpoints are
tomorrow-targets for the same axis. Each pin is an exact-cardinality
assert on the top-level body keys (`{ success, data }`), so a future
PR that adds an internal field intended for a debug/admin rendering
path but inadvertently leaks it on the public endpoint will fail the
corresponding probe.

## Open follow-ups — not auto-fixed

Same as 2026-05-12 plus carryovers:

- **Sandbox corruption on Edit-tool calls — DID NOT RECUR TODAY**.
  Five occurrences in 8 days (2026-05-01 morning, 2026-05-05 morning,
  2026-05-06, 2026-05-07, 2026-05-08), then FIVE consecutive clean
  runs (2026-05-09, 2026-05-10, 2026-05-11, 2026-05-12, 2026-05-13).
  The mitigation (single atomic Python rewrite from the bash sandbox)
  is standard and working. Worth filing an explicit infrastructure
  ticket — same recommendation as 2026-05-06 onwards. **Status:
  dormant — five-day clean streak.**
- **Sandbox `ENOSPC` — tenth day in a row**: the `/sessions` mount
  is still at 100% disk usage, blocking `npx`, `npm install`, and any
  attempt to install or run jest. `node_modules/.bin/tsx` and
  `node_modules/.bin/jest` are also absent. This is an infrastructure
  issue that no per-day QA run can fix from inside the sandbox.
  **Status: unresolved.**
- The 2026-04-29 `user_activity_events` audit-table migration shipped,
  but nothing in `apps/api/src/` or `apps/web/src/` writes to the
  table or reads from `v_user_activity` yet. Code-side gap, not
  test-side. **Carried since 2026-04-30.**
- `CLAUDE.md` still documents `GET /api/analytics` as an authed
  analytics summary endpoint. The route is intentionally gone (smoke
  pins it as a 404). Doc cleanup deferred. **Carried since 2026-05-01.**
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user —
  only empty-body 400 is probed. Requires DB seed to test, deferred.
  **Carried since 2026-05-03.**
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape (`{ success: false, error: { code:
  'RATE_LIMITED', … } }`). The 429 boundary itself remains unpinned
  because hitting it from a smoke probe would require ~60 sequential
  requests. **Carried from 2026-05-06.**
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires an
  auth-token strategy in the smoke test (the handler is
  `requireAuth`-gated, so an unauth probe gets a 401 from middleware
  before the handler ever calls `setHeader`). **Carryover — needs
  design: either a long-lived smoke-test JWT in the env, or a
  dedicated `/api/dev/health-token` endpoint behind an env flag.**
- OPTIONS preflight on the **last** mounted surface
  (`/api/notifications/dispatch`) — to close the per-router pin
  matrix at 9-of-9 routers. **Tomorrow's primary target.**
- Response-shape pin on the remaining 5 endpoints (`insurance`,
  `insurability`, `carriers`, `walkscore`, `public-data`). Pick
  `/:id/insurance` next — same direct-cache-output shape as
  `/:id/risk`, natural fourth pin. **Tomorrow's secondary target.**
- Handler-internals pinning: assert that a `?refresh=true` call
  followed immediately by a default call returns a fresh response,
  not the cached one (exercises the server-side cache invalidation,
  not just the response header). **Carryover from 2026-05-11.**

## PR — next step for the user

The sandbox running this scheduled task does not have GitHub push
credentials (no `gh` CLI, no credential helper, no `GH_TOKEN` env), so
the branch is staged on disk; the follow-up is to push and open the
PR.

### Important: broken local ref to clean up first

During this run the bash sandbox attempted a `git push` via the
`file://` protocol from a fresh clone at `/tmp/cg-clone` (canonical
commit `0169035` lives there with all objects intact, `git fsck` clean,
~7.9 MB). The push transferred the commit object but the mount-level
permissions on the user's `.git/objects/` directory blocked the tree
write — leaving a partially-written ref:

```
C:\CoverGuard Repo Clone\Main\.git\refs\heads\claude\daily-qa-2026-05-13
  -> 0169035 (commit object exists, tree object is incomplete)
```

`git diff main...claude/daily-qa-2026-05-13` on the user's repo fails
with `fatal: unable to read tree 56fecfcaf2...`. The sandbox could not
delete the ref either (`Operation not permitted` on the .lock file).

**To clean this up before pushing**, run from a Windows terminal:

```powershell
cd "C:\CoverGuard Repo Clone\Main"
git update-ref -d refs/heads/claude/daily-qa-2026-05-13
```

(That removes the broken ref. The 3 modified files on disk in the
working tree are intact and unaffected.) The Chrome browser tab can be opened to the PR-creation URL for
`main...claude/daily-qa-2026-05-13`, which will render the diff once
the branch is pushed:

`https://github.com/Cover-Guard/Main/compare/main...claude/daily-qa-2026-05-13?quick_pull=1`

Note: yesterday's branch (`claude/daily-qa-2026-05-12`) may still be
in a similar state. Today's PR layers on top — when the user pushes,
they should choose either:

  (a) a fresh branch from main with both days' commits squashed
      (cleaner history), or
  (b) a stacked PR — push 2026-05-12 first, then 2026-05-13 onto it
      (preserves the day-by-day audit trail).

To finish the run with option (b):

```powershell
cd "C:\CoverGuard Repo Clone\Main"

# Commit yesterday's pending changes on its own branch first (skip if
# you already pushed 2026-05-12 yesterday).
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-12.test.ts `
        ../daily-qa-2026-05-12-report.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-12"
git push -u origin claude/daily-qa-2026-05-12

# Branch off it for today's pass.
git checkout -b claude/daily-qa-2026-05-13
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-13.test.ts `
        daily-qa-2026-05-13.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-13

- No bug fix needed today: smoke-test.ts started clean for the FIFTH
  consecutive day. The recurring Edit-tool corruption (5 occurrences
  in 8 days) did NOT recur. Mitigation reused: today's edits applied
  via single atomic Python rewrite from the bash sandbox.
- Add 2 new probes growing two coverage axes in parallel:
  OPTIONS /api/stripe/subscription allowed-origin (8th surface in the
  per-router CORS pin matrix; 8-of-9 routers now covered),
  GET /:id/risk response-shape exact top-level cardinality (3rd
  member on the response-shape pinning axis at 3-of-9).
- Tighten file-integrity guard: line-count floor 1600 -> 1700;
  carry forward the balanced-braces structural guard; carry forward
  'carriers shou' truncation-marker detection and 'verage' orphan-
  fragment check; cross-check that on-disk runTest count >= 94;
  pin the per-router CORS matrix at 8-of-9; document the response-
  shape pin matrix at 3-of-9.
- Document carryover: notifications router still unpinned on the
  CORS matrix (the last 1 of 9). report.pdf Cache-Control still
  unpinned (auth-gated, needs token strategy). 5 endpoints unpinned
  on the response-shape axis."
git push -u origin claude/daily-qa-2026-05-13
```

Then open: https://github.com/Cover-Guard/Main/compare/main...claude/daily-qa-2026-05-13?quick_pull=1

Or with `gh` CLI:

```powershell
gh pr create --base main --head claude/daily-qa-2026-05-13 `
  --title "chore(qa): daily smoke + QA suite for 2026-05-13" `
  --body-file ../PR_DESCRIPTION-2026-05-13.md
```

The PR description for GitHub is at `outputs/PR_DESCRIPTION-2026-05-13.md`.

## Tomorrow's targets

- OPTIONS preflight on the **last** mounted surface
  (`/api/notifications/dispatch`). Grows the per-router CORS pin
  matrix from 8-of-9 to **9-of-9 (CLOSED)**.
- Response-shape pin on `/api/properties/:id/insurance` — same direct
  cache-output shape as `/:id/risk`, natural fourth pin on the
  response-shape axis. Grows the response-shape matrix from 3-of-9 to
  4-of-9.
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires an
  auth-token strategy. Either a long-lived smoke-test JWT in the env,
  or a dedicated `/api/dev/health-token` endpoint behind an env flag.
  Closes the cached-endpoint matrix at 8-of-8.
- Handler-internals pinning: assert that a `?refresh=true` call
  followed immediately by a default call returns a fresh response,
  not the cached one. Exercises the server-side cache invalidation
  (the second half of the cache-invalidation contract, after the
  response-header half closed at 6-of-6 on 2026-05-10).
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape. Carried from 2026-05-06.
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user
  (carried since 2026-05-03 — needs DB seed).
- File an infrastructure ticket for: (a) the recurring Edit-tool
  corruption around 30 KB (dormant for **five** consecutive days now,
  but mitigation lives outside the test code), and (b) the
  `/sessions` 100% disk usage that's been blocking jest for **ten**
  days running. Both are multi-day patterns that no single per-day
  QA run can resolve.
