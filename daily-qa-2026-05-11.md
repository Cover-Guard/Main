# Daily smoke + QA run — 2026-05-11

Scheduled task: `daily-smokeqa-testing`. Today's run had no bug fixes —
the smoke-test.ts file started clean for the **third consecutive day**
(1395 lines from yesterday's state, 88 probes intact, no truncation
marker, no orphan fragments, brace balance 433/433). The recurring
Edit-tool corruption that bit the file FIVE times in 8 days did NOT
recur today either. The whole run was coverage expansion: a 2-probe
pass that grows the **per-router CORS pin matrix from 5-of-9 to 6-of-9
routers** (`OPTIONS /api/deals`, sixth distinct router file) and
**initiates the response-shape pinning axis** (`/report` exact-key
cardinality) now that the refresh=true matrix was closed yesterday at
6-of-6.

## What ran

| Suite | Status | Notes |
|---|---|---|
| `npx tsx scripts/qa/smoke-test.ts` | **fail-to-fetch** | Same sandbox constraint as 2026-05-02 / 03 / 04 / 05 / 06 / 07 / 08 / 09 / 10 — no live API on :4000. Plus today, `npx` itself returns `ENOSPC` again (sandbox `/sessions` is at 100% disk usage — ninth day in a row). The TypeScript bundle in `node_modules/typescript/lib/typescript.js` is missing as well; `node_modules/.bin/tsx` and `node_modules/.bin/jest` are also absent. Static structural review only. |
| Static structural review of `smoke-test.ts` | **passed (no fix needed)** | Initial state on disk: 1395 lines (post-2026-05-10 state), ends with `})` after `run().catch(...)` block, 88 `await runTest(` invocations, no `carriers shou$` truncation marker, no `verage` orphan at column 1 (only references in changelog comments), balanced braces (433 open / 433 close). The file was preserved cleanly from 2026-05-10. |
| `npx tsx scripts/qa/health-check.ts` | not executed | no live API in sandbox |
| `npx tsx scripts/qa/env-validate.ts` | not executed | no `.env` in sandbox |
| `apps/api` jest suite | not executed | `node_modules/.bin/jest` shim missing in mount; `npx jest` blocked by `ENOSPC`; same constraint as prior runs. |

## Bugs fixed (0)

The recurring Edit-tool corruption did **not** recur today. Three
consecutive clean days. The mitigation established 2026-05-09 (single
atomic Python rewrite from the bash sandbox) was reused today and
again worked cleanly. Today's edits were applied via
`python3 /tmp/patch_smoke_05_11.py` — three sequential `str.replace`
operations (header changelog + OPTIONS deals probe + /report response-
shape probe) followed by one atomic `PATH.write_text(...)`. Pre- and
post-write integrity checks (line count, probe count, run().catch()
count, brace balance, truncation-marker absence) all passed.

This sidesteps the recurring `~30 KB sequential-Edit truncation` bug
entirely, and remains the documented workflow for any modification to
`smoke-test.ts` going forward.

Static review of all nine routers (advisor, alerts, auth, clients,
dashboard, deals, notifications, properties, stripe) found no missing
`requireAuth` and no mismatched verb pairs. CORS configuration in
`apps/api/src/index.ts` remains correct. The cached-endpoint Cache-
Control matrix is unchanged at **7 of 8 endpoints** (only
`/:id/report.pdf` remains, and it's auth-gated). The refresh=true
matrix remains **CLOSED at 6-of-6**.

## Coverage gaps closed (2 new probes + tightened file-integrity guard)

`Main/scripts/qa/smoke-test.ts` (1510 lines, 90 probes, +2 from yesterday):

1. **`OPTIONS /api/deals` from allowed origin → 204, ACAO echoed
   (and not `*`)** — sixth mounted surface in the per-router CORS pin
   matrix, beyond `/api/properties/search` (2026-05-06), `/api/auth/me`
   (2026-05-07), `/api/clients` (2026-05-08), `/api/dashboard`
   (2026-05-09), and `/api/advisor/chat` (2026-05-10). `deals.ts` is
   yet another router file, and `/api/deals` is the agent's revenue
   pipeline. Note: `deals.ts` does `dealsRouter.use(requireAuth)`
   router-wide so every handler is auth-gated, but the OPTIONS
   preflight goes through the global `cors()` middleware in
   `index.ts` BEFORE `requireAuth` fires (Express short-circuits
   OPTIONS when the CORS middleware emits a 204). A per-router CORS
   override on the deals router would let any origin credential-sniff
   the deal pipeline (deal value, client name, carrier, fallout
   reason) via a XHR initiated from a victim's browser session — a
   CSRF-via-CORS regression into the agent's revenue pipeline.
   **Per-router pin matrix is now 6-of-9 routers.**

2. **`GET /api/properties/:id/report` response shape: exactly 2 top-
   level keys, exactly 6 data keys** — initiates the **response-shape
   pinning axis**. The existing /report probe (added 2026-05-04)
   already asserts that each of the 6 documented data keys is PRESENT
   (`property`, `risk`, `insurance`, `insurability`, `carriers`,
   `publicData`); today's probe additionally asserts that no EXTRA
   keys leaked. The regression class is: a future PR adds an internal
   field to the report bundle (e.g. `ownerEmail`, `apiCallCount`,
   `computeMillis`, `internalDebugBlob`, an upstream API key that was
   supposed to be redacted before egress) intended for a debug/admin
   rendering path but inadvertently leaks it on the public `/report`
   endpoint. Existing 'has property + has risk + ...' probes are
   silent on extras; an exact-cardinality assert is what catches the
   leak. Pins both the top-level body keys (`{ success, data }`) and
   data keys (`{ property, risk, insurance, insurability, carriers,
   publicData }`).

`Main/apps/api/src/__tests__/daily-review-2026-05-11.test.ts` (722
lines, 8 describe blocks, 41 it blocks):

3. **Smoke-test surface contract (2026-05-11)** — pins the 90-entry
   `REQUIRED_SMOKE_PROBES_05_11` array (≥90 probes, strictly grows
   over the 88 of 2026-05-10, every entry uniquely worded, today's
   2 additions are each individually asserted). Reverse-pin: every
   `refresh=true` probe (still 6) must NOT assert `s-maxage=`.
4. **Smoke-test file integrity** — carries forward the 2026-05-10
   widened guard. Tightens line-count floor from 1300 to 1400.
   Carries forward the `carriers shou$` truncation-marker check.
   Adds an extra cross-check: at least 90 `runTest(` invocations on
   disk (must match the contract list above). Retains the balanced-
   braces structural-corruption guard.
5. **OPTIONS /api/deals preflight** — documents the per-router pin
   matrix invariant (6 of 9 routers) and asserts 7 OPTIONS-prefixed
   probes total (search disallowed + search allowed + auth/me +
   clients + dashboard + advisor + deals). Documents the three
   remaining router targets (alerts, stripe, notifications) as
   carryovers — pinning them as *negative* assertions so a future PR
   adding the probe will need to update the expectation.
6. **Property-id-bound Cache-Control contracts** — documents the
   8-endpoint Cache-Control matrix (7 cached pinned, 1 unpinned —
   `report.pdf` is auth-gated). Documents the 6-endpoint refresh=true
   matrix (6 pinned — **CLOSED**, carried). Asserts ≥7 `s-maxage=N`
   cache pins and exactly 6 refresh=true no-cache pins.
7. **Refresh-branch matrix completeness (carried CLOSED today)** —
   asserts that the 6 refresh=true probes specifically cover risk +
   insurance + carriers + insurability + walkscore + public-data.
   The completeness check iterates the 6 cached roots and asserts
   each has a corresponding refresh-branch probe.
8. **Verb-parity audit** — carryover from 2026-05-03. No new mutating
   verbs today (today's additions are an OPTIONS preflight + a GET
   response-shape probe). Asserts that every PATCH/DELETE probe in
   the surface contract ends with `→ 401`.
9. **CORS configuration sanity** — documents the per-router pin
   matrix (6-of-9 routers). Asserts ≥7 OPTIONS-prefixed probes and
   confirms each of the six pinned surfaces appears.
10. **Response-shape pinning axis (NEW — initiated today)** — asserts
    exactly 1 response-shape probe today (`/report`), documents the
    6 endpoints that remain unpinned on this axis (risk, insurance,
    insurability, carriers, walkscore, public-data) as negative
    pins so a future PR adding response-shape probes for them will
    need to update the expectation, and asserts the initial probe
    text references both `2 top-level keys` and `6 data keys` so a
    future cleanup that drops one assertion also updates the
    contract.

## Files touched

- `Main/scripts/qa/smoke-test.ts` — applied 2 new probes via a single
  atomic Python rewrite (run from `/tmp/patch_smoke_05_11.py`). Grew
  from 1395 lines / 88 probes / ~70 KB to 1510 lines / 90 probes /
  ~77 KB. Added the 2026-05-11 changelog block at the top, the
  `OPTIONS /api/deals` always-on probe at line ~666 (after the 0h
  advisor probe), and the `/report` response-shape probe at line
  ~1032 (after the existing `/report` bundle probe). The cached-
  endpoint matrix is unchanged at 7 of 8 endpoints (only `report.pdf`
  remains, auth-gated). The refresh=true matrix is unchanged at
  CLOSED 6 of 6.
- `Main/apps/api/src/__tests__/daily-review-2026-05-11.test.ts` (new)
  — pinned probe list (90 entries), tightened file-integrity guard
  (line-count floor 1300 → 1400, today's runTest cross-check, brace-
  balance guard), per-router CORS pin matrix doc (6-of-9), cached-
  endpoint Cache-Control matrix doc, refresh-branch matrix doc with
  the completeness check (6-of-6 closed), new response-shape pinning
  axis doc.
- `Main/daily-qa-2026-05-11.md` (new) — this report.

## Probe count over time

| Date | Probes | Delta | Notes |
|---|---|---|---|
| 2026-04-30 | ~28 | baseline | search → 401, /health, /robots.txt, suggest, ticker, deals, alerts, vapid |
| 2026-05-01 morning | ~32 | +4 | analytics → 404, advisor 401, geocode 400, walkscore 404 |
| 2026-05-01 evening | ~46 | +14 | full unauth-401 set, register 400, totally-fake 404, carriers happy-path |
| 2026-05-02 | ~47 | +14 (write-side + property-id-bound) | PATCH/DELETE /me, POST clients/deals, ack alert, walkscore 200, public-data, report.pdf 401, checklists, save, quote-request |
| 2026-05-03 | ~53 | +9 | PATCH/DELETE clients/deals, bare detail 404, webhook 400, save DELETE, checklists PATCH/DELETE |
| 2026-05-04 | 56 | +3 | unauth /report 404, /report.pdf 404 (NOT 401), /report 200 |
| 2026-05-05 | 61 | +5 | HEAD /health, GET /properties 404, GET webhook 404, POST search 404, HEAD /report.pdf 401 |
| 2026-05-06 | 68 | +7 | OPTIONS preflight × 2, geocode boundaries × 2, HEAD bogus /report.pdf 404 |
| 2026-05-07 | 74 | +6 | suggest CC, suggest standard rate-limit headers, OPTIONS /api/auth/me, /:id CC, /:id/risk CC |
| 2026-05-08 | 80 | +6 | OPTIONS /api/clients, walkscore CC, insurance CC, insurability CC, carriers CC (1h NOT 2h), risk?refresh=true no-cache |
| 2026-05-09 | 86 | +6 | OPTIONS /api/dashboard, public-data CC, insurance?refresh=true, carriers?refresh=true, insurability?refresh=true, walkscore?refresh=true |
| 2026-05-10 | 88 | +2 | OPTIONS /api/advisor/chat (5th-router CORS pin), public-data?refresh=true (closes refresh matrix) |
| 2026-05-11 | 90 | +2 | OPTIONS /api/deals (6th-router CORS pin), /report response-shape (initiates response-shape axis) |

## Cached-endpoint Cache-Control matrix (status as of 2026-05-11)

| Endpoint | TTL (s-maxage) | Cached pinned? | Refresh branch pinned? |
|---|---|---|---|
| `GET /api/properties/suggest` | 300 (5 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id` | 1800 (30 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id/risk` | 7200 (2 h) | ✅ since 2026-05-07 | ✅ since 2026-05-08 |
| `GET /api/properties/:id/insurance` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/insurability` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/carriers` | 3600 (1 h, NOT 2h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/walkscore` | 86400 (24 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/public-data` | 86400 (24 h) | ✅ since 2026-05-09 | ✅ since 2026-05-10 |
| `GET /api/properties/:id/report.pdf` | private, max-age=300 | ❌ requires auth-token strategy | n/a (auth-gated, never CDN-cached) |

Cached-side: **7 of 8 endpoints pinned** (unchanged). Refresh-side:
**6 of 6 endpoints pinned (CLOSED — carried)**. Total Cache-Control
matrix: **13 of 14 cells pinned (93%) — unchanged from 2026-05-10**.

## Per-router CORS pin matrix (status as of 2026-05-11)

| Router file | Mounted surface | Pinned by smoke probe? |
|---|---|---|
| `properties.ts` | `OPTIONS /api/properties/search` | ✅ since 2026-05-06 (allowed + denied) |
| `auth.ts` | `OPTIONS /api/auth/me` | ✅ since 2026-05-07 |
| `clients.ts` | `OPTIONS /api/clients` | ✅ since 2026-05-08 |
| `dashboard.ts` | `OPTIONS /api/dashboard/ticker` | ✅ since 2026-05-09 |
| `advisor.ts` | `OPTIONS /api/advisor/chat` | ✅ since 2026-05-10 |
| `deals.ts` | `OPTIONS /api/deals` | ✅ since 2026-05-11 |
| `alerts.ts` | `OPTIONS /api/alerts/carrier-exits` | ❌ tomorrow's target |
| `stripe.ts` | `OPTIONS /api/stripe/subscription` | ❌ |
| `notifications.ts` | `OPTIONS /api/notifications/dispatch` | ❌ |

**6 of 9 routers pinned.** Each pin is on a different router file,
so a per-router CORS override on any single pinned router would fail
at least one probe. The remaining 3 are tomorrow-targets (pick at
least 1 to grow to 7-of-9).

## Response-shape pin matrix (NEW — status as of 2026-05-11)

| Endpoint | Documented data keys | Response-shape pinned? |
|---|---|---|
| `GET /api/properties/:id/report` | property + risk + insurance + insurability + carriers + publicData (6) | ✅ since 2026-05-11 |
| `GET /api/properties/:id` | property | ❌ tomorrow's target |
| `GET /api/properties/:id/risk` | risk profile | ❌ |
| `GET /api/properties/:id/insurance` | insurance estimate | ❌ |
| `GET /api/properties/:id/insurability` | insurability status | ❌ |
| `GET /api/properties/:id/carriers` | carriers list (varying shape — array or { carriers: [] }) | ❌ |
| `GET /api/properties/:id/walkscore` | walk/transit/bike scores | ❌ |
| `GET /api/properties/:id/public-data` | images + tax + listings + amenities (aggregate) | ❌ |
| `GET /api/properties/suggest` | suggestions array | ❌ |

**1 of 9 endpoints pinned.** The 8 remaining endpoints are
tomorrow-targets for the same axis. Each pin is an exact-cardinality
assert on both top-level body keys (`{ success, data }`) and data
keys, so a future PR that adds an internal field intended for a
debug/admin rendering path but inadvertently leaks it on the public
endpoint will fail the corresponding probe.

## Open follow-ups — not auto-fixed

Same as 2026-05-10 plus carryovers, with several resolved today:

- **Sandbox corruption on Edit-tool calls — DID NOT RECUR TODAY**.
  Five occurrences in 8 days (2026-05-01 morning, 2026-05-05 morning,
  2026-05-06, 2026-05-07, 2026-05-08), then THREE consecutive clean
  runs (2026-05-09, 2026-05-10, 2026-05-11). The mitigation (single
  atomic Python rewrite from the bash sandbox) is standard and
  working. Worth filing an explicit infrastructure ticket — same
  recommendation as 2026-05-06 onwards. **Status: dormant — keep
  watching, three-day clean streak.**
- **Sandbox `ENOSPC` — ninth day in a row**: the `/sessions` mount
  is still at 100% disk usage, blocking `npx`, `npm install`, and any
  attempt to install or run jest. The TypeScript bundle in
  `node_modules/typescript/lib/typescript.js` is still missing.
  `node_modules/.bin/tsx` and `node_modules/.bin/jest` are also
  absent. This is an infrastructure issue that no per-day QA run can
  fix from inside the sandbox. **Status: unresolved.**
- The 2026-04-29 `user_activity_events` audit-table migration shipped,
  but nothing in `apps/api/src/` or `apps/web/src/` writes to the
  table or reads from `v_user_activity` yet. Code-side gap, not
  test-side. **Carried since 2026-04-30.**
- `CLAUDE.md` still documents `GET /api/analytics` as an authed
  analytics summary endpoint. The route is intentionally gone (smoke
  pins it as a 404). Doc cleanup deferred. **Carried since 2026-05-01.**
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user —
  only empty-body 400 is probed. Requires DB seed to test, deferred.
  **Carried since 2026-05-03.**
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape (`{ success: false, error: { code:
  'RATE_LIMITED', … } }`). The 429 boundary itself remains unpinned
  because hitting it from a smoke probe would require ~60 sequential
  requests. **Carried from 2026-05-06.**
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires an
  auth-token strategy in the smoke test (the handler is
  `requireAuth`-gated, so an unauth probe gets a 401 from middleware
  before the handler ever calls `setHeader`). **Carryover — needs
  design: either a long-lived smoke-test JWT in the env, or a
  dedicated `/api/dev/health-token` endpoint behind an env flag.**
- OPTIONS preflight on a seventh mounted surface (`/api/alerts`,
  `/api/stripe`, or `/api/notifications`) — to grow the per-router
  pin matrix from 6-of-9 to 7-of-9 routers. **New tomorrow-target
  (3 of 9 remain).**
- Response-shape pin on the remaining 8 endpoints (the matrix initiated
  today is 1-of-9). Pick `/:id` next — it's the lightest payload and
  the natural second pin. **New tomorrow-target.**
- Handler-internals pinning: assert that a `?refresh=true` call
  followed immediately by a default call returns a fresh response,
  not the cached one (exercises the server-side cache invalidation,
  not just the response header). **New tomorrow-target — the next
  axis on the cache contract after refresh=true matrix closed at
  6-of-6.**

## PR — next step for the user

The sandbox running this scheduled task may or may not have git push
access depending on day. Today's diff is staged on disk; the
follow-up is to push the branch and open the PR. A Chrome browser
tab will be opened to the PR-creation URL for
`main...claude/daily-qa-2026-05-11`, which will render the diff once
the branch is pushed:
`https://github.com/Cover-Guard/Main/compare/main...claude/daily-qa-2026-05-11?quick_pull=1`

Note: yesterday's branch (`claude/daily-qa-2026-05-10`) may still be
checked out locally with uncommitted changes in the index. Today's PR
layers on top — when the user pushes, they should choose either:
  (a) a fresh branch from main with both days' commits squashed
      (cleaner history), or
  (b) a stacked PR — push 2026-05-10 first, then 2026-05-11 onto it
      (preserves the day-by-day audit trail).

To finish the run with option (b):

```powershell
cd "C:\CoverGuard Repo Clone\Main"

# Commit yesterday's pending changes on its own branch first (skip if
# you already pushed 2026-05-10 yesterday).
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-10.test.ts `
        daily-qa-2026-05-10.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-10"
git push -u origin claude/daily-qa-2026-05-10

# Branch off it for today's pass.
git checkout -b claude/daily-qa-2026-05-11
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-11.test.ts `
        daily-qa-2026-05-11.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-11

- No bug fix needed today: smoke-test.ts started clean for the third
  consecutive day. The recurring Edit-tool corruption (5 occurrences
  in 8 days) did NOT recur. Mitigation reused: today's edits applied
  via single atomic Python rewrite from the bash sandbox.
- Add 2 new probes growing two coverage axes in parallel:
  OPTIONS /api/deals allowed-origin (6th surface in the per-router
  CORS pin matrix; 6-of-9 routers now covered),
  GET /:id/report response-shape exact-key-cardinality (initiates
  the response-shape pinning axis at 1-of-9).
- Tighten file-integrity guard: line-count floor 1300 -> 1400;
  carry forward the balanced-braces structural guard; carry forward
  'carriers shou' truncation-marker detection; cross-check that
  on-disk runTest count >= 90; pin the per-router CORS matrix at
  6-of-9; document the new response-shape pin matrix at 1-of-9.
- Document carryover: 3 router files (alerts, stripe, notifications)
  still unpinned on the CORS matrix. report.pdf Cache-Control still
  unpinned (auth-gated, needs token strategy). 8 endpoints unpinned
  on the response-shape axis."
git push -u origin claude/daily-qa-2026-05-11
```

## Suggested PR body

```markdown
## What

Daily smoke + QA pass for 2026-05-11. No bug fix + two new probes +
tightened file-integrity guard. **Response-shape pinning axis
initiated; per-router CORS pin matrix grows to 6-of-9.**

## Why

**No bug fix this run.** `scripts/qa/smoke-test.ts` started clean for
the third consecutive day (1395 lines, 88 probes, no truncation, no
orphan fragments, brace balance 433/433). The Edit-tool corruption
that bit the file FIVE times in 8 days (2026-05-01 morning,
2026-05-05 morning, 2026-05-06, 2026-05-07, 2026-05-08) did NOT recur
today either. The mitigation established 2026-05-09 (single atomic
Python rewrite from the bash sandbox) was reused today and worked
cleanly again.

**Coverage expansion.** Two new probes growing two coverage axes in
parallel:

1. `OPTIONS /api/deals` allowed-origin → 204, ACAO echoed (sixth
   mounted surface in the per-router CORS pin matrix; 6-of-9 routers
   now covered). Three routers remain unpinned: alerts, stripe,
   notifications. Note: deals.ts auth-gates every handler via
   `dealsRouter.use(requireAuth)`, but the OPTIONS preflight goes
   through cors() before requireAuth fires.
2. `GET /api/properties/:id/report` response shape: exactly 2 top-
   level keys, exactly 6 data keys — initiates the response-shape
   pinning axis. The existing /report probe already asserts that
   each of the 6 documented data keys is PRESENT; this new probe
   additionally asserts that no EXTRA keys leaked. Catches the
   regression class where an internal debug field intended for an
   admin rendering path inadvertently leaks on the public /report
   endpoint.

**Tightened guard.** `daily-review-2026-05-11.test.ts` carries
forward the 2026-05-10 file-integrity guard and:

- raises the line-count floor from 1300 to 1400
- carries forward the structural balanced-braces guard
- carries forward the `carriers shou$` truncation-marker check
- adds a cross-check that on-disk `runTest(` count is at least 90
- documents the per-router CORS pin matrix (6-of-9 routers now
  covered)
- documents the response-shape pin matrix (1-of-9 endpoints — new
  axis)
- documents the cached-endpoint Cache-Control matrix (7-of-8 cached
  pinned + 6-of-6 refresh pinned = 13-of-14 cells, 93% — unchanged)
- adds negative pins on the three remaining router files (alerts,
  stripe, notifications) and the eight unpinned response-shape
  endpoints

## Files

- `scripts/qa/smoke-test.ts` — atomic Python rewrite, +2 new probes
  (1510 lines, 90 probes).
- `apps/api/src/__tests__/daily-review-2026-05-11.test.ts` — pins
  the 90-entry probe list, tightened file-integrity guard, contract
  docs.

## Testing

Static review only — sandbox can't bring up a live API and `/sessions`
is at 100% disk so `npx` / `npm install` are blocked (ninth day).
Once the dev stack is up:

```bash
npm run qa:smoke -- --property-id <real-uuid>
npm test --workspace apps/api
```
```

## Tomorrow's targets

- OPTIONS preflight on a seventh mounted surface — `/api/alerts`,
  `/api/stripe`, or `/api/notifications`. Grows the per-router CORS
  pin matrix from 6-of-9 to 7-of-9.
- Response-shape pin on `/api/properties/:id` (the lightest payload,
  natural second pin on the response-shape axis). Grows the response-
  shape matrix from 1-of-9 to 2-of-9.
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires an
  auth-token strategy. Either a long-lived smoke-test JWT in the env,
  or a dedicated `/api/dev/health-token` endpoint behind an env flag.
  Closes the cached-endpoint matrix at 8-of-8.
- Handler-internals pinning: assert that a `?refresh=true` call
  followed immediately by a default call returns a fresh response,
  not the cached one. Exercises the server-side cache invalidation
  (the second half of the cache-invalidation contract, after the
  response-header half closed at 6-of-6 on 2026-05-10).
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape. Carried from 2026-05-06.
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user
  (carried since 2026-05-03 — needs DB seed).
- File an infrastructure ticket for: (a) the recurring Edit-tool
  corruption around 30 KB (dormant for three consecutive days now,
  but mitigation lives outside the test code), and (b) the
  `/sessions` 100% disk usage that's been blocking jest for nine
  days running. Both are multi-day patterns that no single per-day
  QA run can resolve.
