# Daily smoke + QA run — 2026-05-09

Scheduled task: `daily-smokeqa-testing`. Today's run had no bug fixes —
the smoke-test.ts file started clean (1085 lines from yesterday's
restored state, 80 probes intact, no truncation marker, no orphan
fragments). The recurring Edit-tool corruption that bit the file FIVE
times in 8 days did NOT recur today. The whole run was coverage
expansion: a 6-probe pass that closes every cached-endpoint Cache-
Control gap except `/:id/report.pdf` (auth-gated, requires a token
strategy) and closes 5 of 6 refresh=true no-cache branches in a single
`--property-id` run.

## What ran

| Suite | Status | Notes |
|---|---|---|
| `npx tsx scripts/qa/smoke-test.ts` | **fail-to-fetch** | Same sandbox constraint as 2026-05-02 / 03 / 04 / 05 / 06 / 07 / 08 — no live API on :4000. Plus today, `npx` itself returned `ENOSPC` again (sandbox `/sessions` is at 100% disk usage — seventh day in a row). The TypeScript bundle in `node_modules/typescript/lib/typescript.js` is missing as well. Static structural review only. |
| Static structural review of `smoke-test.ts` | **passed (no fix needed)** | Initial state on disk: 1085 lines, ends with `})` after `run().catch(...)` block, 80 `await runTest(` invocations, no `carriers shou$` truncation marker, no `verage` orphan, balanced braces (373 open / 373 close). The file was preserved cleanly from 2026-05-08. |
| `npx tsx scripts/qa/health-check.ts` | not executed | no live API in sandbox |
| `npx tsx scripts/qa/env-validate.ts` | not executed | no `.env` in sandbox |
| `apps/api` jest suite | not executed | `node_modules/.bin/jest` shim missing in mount; `npx jest` blocked by `ENOSPC`; same constraint as prior runs. |

## Bugs fixed (0)

The recurring Edit-tool corruption did **not** recur today. To keep it
that way, today's edits to `smoke-test.ts` were applied via a single
atomic Python rewrite (run from the bash sandbox via
`python3 /tmp/patch_smoke.py`) instead of a chain of Edit-tool calls.
The Python script does the equivalent of 3 sequential `str.replace`
operations (header changelog + OPTIONS /api/dashboard probe + 5
property-id-bound probes), then writes the whole file back in one
syscall. Pre- and post-write integrity checks (line count, probe
count, run().catch() count, brace balance, truncation-marker absence)
all passed.

This sidesteps the recurring `~30 KB sequential-Edit truncation` bug
entirely, and is now the documented workflow for any modification to
`smoke-test.ts` going forward. Yesterday's recommendation
("single-Write of the full file") is generalized: any bulk-edit
operation on this file should be applied atomically from the bash
sandbox, NOT via a chain of Edit-tool calls.

Static review of all nine routers (advisor, alerts, auth, clients,
dashboard, deals, notifications, properties, stripe) found no missing
`requireAuth` and no mismatched verb pairs. CORS configuration in
`apps/api/src/index.ts` remains correct. The cached-endpoint Cache-
Control matrix is now pinned across **7 of 8 endpoints** (only
`/:id/report.pdf` remains, and it's auth-gated). The refresh=true
matrix is pinned on **5 of 6 endpoints** (only
`/:id/public-data?refresh=true` remains).

## Coverage gaps closed (6 new probes + tightened file-integrity guard)

`Main/scripts/qa/smoke-test.ts` (1294 lines, 86 probes, +6 from yesterday):

1. **`OPTIONS /api/dashboard` from allowed origin → 204, ACAO echoed
   (and not `*`)** — fourth mounted surface in the per-router cors
   pin matrix, beyond `/api/properties/search` (2026-05-06),
   `/api/auth/me` (2026-05-07), and `/api/clients` (2026-05-08).
   `dashboard.ts` is yet another router file, and the dashboard
   surface is where the agent's deal-pipeline ticker, KPI cards, and
   recent-activity feed are served. A per-router cors override on
   the dashboard router would let any origin scrape pipeline numbers
   via a credentialed XHR. **Per-router pin matrix is now 4-of-9
   routers.**

2. **`GET /api/properties/:id/public-data` Cache-Control includes
   `s-maxage=86400` and `public`** — pins the 24-hour CDN cache on
   public-data. public-data is the heaviest aggregate read (images +
   tax + listings + amenities); a regression that drops
   `setCacheHeaders` would force every property-detail page-load
   through the full upstream-fan-out. Default branch only —
   `refresh=true` emits no-cache (probe (e) below). **Closes the
   penultimate gap in the cached-endpoint matrix.**

3. **`GET /api/properties/:id/insurance?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=`** — pins the
   `setNoCacheHeaders` branch on `/insurance`. Symmetric to the
   `/risk?refresh=true` probe added 2026-05-08. The refresh branch
   invalidates the property's insurance cache before returning; a
   regression that drops `setNoCacheHeaders` would let the CDN
   serve a 7200s stale response after the caller explicitly asked
   for a fresh recompute.

4. **`GET /api/properties/:id/carriers?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=`** — pins the
   `setNoCacheHeaders` branch on `/carriers`. Especially important
   given the **VA-01 carrier-exit alert SLA** — a stale carriers
   response after a force-refresh is the exact failure mode the SLA
   exists to prevent.

5. **`GET /api/properties/:id/insurability?refresh=true`
   Cache-Control includes `no-store` AND `private`, NOT
   `s-maxage=`** — pins the `setNoCacheHeaders` branch on
   `/insurability`. Bonus pin — completes 4 of the 5 remaining
   refresh=true branches identified yesterday.

6. **`GET /api/properties/:id/walkscore?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=`** — pins the
   `setNoCacheHeaders` branch on `/walkscore`. **Closes the
   refresh=true matrix on a single `--property-id` run.** Only
   `/public-data?refresh=true` remains unpinned (carryover for
   tomorrow). The probe also handles the 503 fallback gracefully
   (skip header assertion if `status === 503` so the probe doesn't
   false-fail when the upstream is down).

`Main/apps/api/src/__tests__/daily-review-2026-05-09.test.ts` (~520
lines, 7 describe blocks, ~32 it blocks):

7. **Smoke-test surface contract (2026-05-09)** — pins the 86-entry
   `REQUIRED_SMOKE_PROBES_05_09` array (≥86 probes, strictly grows
   over the 80 of 2026-05-08, every entry uniquely worded, today's
   6 additions are each individually asserted). Reverse-pin: every
   `refresh=true` probe must NOT assert `s-maxage=` (regression-
   class catch).
8. **Smoke-test file integrity** — carries forward the 2026-05-08
   widened guard. Tightens line-count floor from 1000 to 1200.
   Carries forward the today-specific `carriers shou$` truncation-
   marker check from yesterday. Adds an extra cross-check: at
   least 86 `runTest(` invocations on disk (must match the contract
   list above).
9. **OPTIONS /api/dashboard preflight** — documents the per-router
   pin matrix invariant (4 of 9 routers) and asserts 5 OPTIONS-
   prefixed probes total (search disallowed + search allowed + auth/
   me + clients + dashboard).
10. **Property-id-bound Cache-Control contracts** — documents the
    8-endpoint Cache-Control matrix (7 cached pinned, 1 unpinned —
    report.pdf is auth-gated). Documents the 6-endpoint refresh=true
    matrix (5 pinned, 1 unpinned — public-data is tomorrow's target).
    Asserts ≥7 `s-maxage=N` cache pins and ≥5 refresh=true no-cache
    pins.
11. **Refresh-branch matrix completeness (new today)** — asserts that
    the 5 refresh=true probes specifically cover risk + insurance +
    carriers + insurability + walkscore (each handler has its own
    cache-invalidation contract). Pins the *negative* assertion that
    `public-data?refresh=true` is NOT yet pinned — a future PR
    adding the probe will need to update this expectation, which is
    desired (that's how the carryover gets tracked).
12. **Verb-parity audit** — carryover from 2026-05-03. No new
    mutating verbs today (today's additions are all GET/OPTIONS
    read-side probes). Asserts that every PATCH/DELETE probe in
    the surface contract ends with `→ 401`.
13. **CORS configuration sanity** — documents the per-router pin
    matrix. Asserts ≥5 OPTIONS-prefixed probes and confirms each of
    the four pinned surfaces appears.

## Files touched

- `Main/scripts/qa/smoke-test.ts` — applied 6 new probes via a
  single atomic Python rewrite (run from `/tmp/patch_smoke.py`).
  Grew from 1085 lines / 80 probes / 11543 bytes to 1294 lines / 86
  probes. Added the 2026-05-09 changelog block at the top, the
  `OPTIONS /api/dashboard` always-on probe at line ~577, and the 5
  property-id-bound probes (1 cached + 4 refresh) at lines ~1094–1209.
  The cached-endpoint matrix now pins 7 of 8 endpoints (only
  `report.pdf` remains, auth-gated). The refresh=true matrix now
  pins 5 of 6 endpoints (only `public-data?refresh=true` remains).
- `Main/apps/api/src/__tests__/daily-review-2026-05-09.test.ts` (new)
  — pinned probe list (86 entries), tightened file-integrity guard
  (line-count floor 1000 → 1200, today's runTest cross-check),
  per-router CORS pin matrix doc (4-of-9), cached-endpoint
  Cache-Control matrix doc, refresh-branch matrix doc with the
  negative pin on `public-data?refresh=true`.
- `Main/daily-qa-2026-05-09.md` (new) — this report.

## Probe count over time

| Date | Probes | Delta | Notes |
|---|---|---|---|
| 2026-04-30 | ~28 | baseline | search → 401, /health, /robots.txt, suggest, ticker, deals, alerts, vapid |
| 2026-05-01 morning | ~32 | +4 | analytics → 404, advisor 401, geocode 400, walkscore 404 |
| 2026-05-01 evening | ~46 | +14 | full unauth-401 set, register 400, totally-fake 404, carriers happy-path |
| 2026-05-02 | ~47 | +14 (write-side + property-id-bound) | PATCH/DELETE /me, POST clients/deals, ack alert, walkscore 200, public-data, report.pdf 401, checklists, save, quote-request |
| 2026-05-03 | ~53 | +9 | PATCH/DELETE clients/deals, bare detail 404, webhook 400, save DELETE, checklists PATCH/DELETE |
| 2026-05-04 | 56 | +3 | unauth /report 404, /report.pdf 404 (NOT 401), /report 200 |
| 2026-05-05 | 61 | +5 | HEAD /health, GET /properties 404, GET webhook 404, POST search 404, HEAD /report.pdf 401 |
| 2026-05-06 | 68 | +7 | OPTIONS preflight × 2, geocode boundaries × 2, HEAD bogus /report.pdf 404 |
| 2026-05-07 | 74 | +6 | suggest CC, suggest standard rate-limit headers, OPTIONS /api/auth/me, /:id CC, /:id/risk CC |
| 2026-05-08 | 80 | +6 | OPTIONS /api/clients, walkscore CC, insurance CC, insurability CC, carriers CC (1h NOT 2h), risk?refresh=true no-cache |
| 2026-05-09 | 86 | +6 | OPTIONS /api/dashboard, public-data CC, insurance?refresh=true, carriers?refresh=true, insurability?refresh=true, walkscore?refresh=true |

## Cached-endpoint Cache-Control matrix (status as of 2026-05-09)

| Endpoint | TTL (s-maxage) | Cached pinned? | Refresh branch pinned? |
|---|---|---|---|
| `GET /api/properties/suggest` | 300 (5 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id` | 1800 (30 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id/risk` | 7200 (2 h) | ✅ since 2026-05-07 | ✅ since 2026-05-08 |
| `GET /api/properties/:id/insurance` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/insurability` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/carriers` | 3600 (1 h, NOT 2h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/walkscore` | 86400 (24 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/public-data` | 86400 (24 h) | ✅ since 2026-05-09 | ❌ tomorrow's target |
| `GET /api/properties/:id/report.pdf` | private, max-age=300 | ❌ requires auth-token strategy | n/a (auth-gated, never CDN-cached) |

Cached-side: **7 of 8 endpoints pinned**. Refresh-side: **5 of 6
endpoints pinned**. Total Cache-Control matrix: **12 of 14 cells
pinned (86%)**.

## Per-router CORS pin matrix (status as of 2026-05-09)

| Router file | Mounted surface | Pinned by smoke probe? |
|---|---|---|
| `properties.ts` | `OPTIONS /api/properties/search` | ✅ since 2026-05-06 (allowed + denied) |
| `auth.ts` | `OPTIONS /api/auth/me` | ✅ since 2026-05-07 |
| `clients.ts` | `OPTIONS /api/clients` | ✅ since 2026-05-08 |
| `dashboard.ts` | `OPTIONS /api/dashboard/ticker` | ✅ since 2026-05-09 |
| `advisor.ts` | `OPTIONS /api/advisor/chat` | ❌ tomorrow's target |
| `deals.ts` | `OPTIONS /api/deals` | ❌ tomorrow's target |
| `alerts.ts` | `OPTIONS /api/alerts/carrier-exits` | ❌ |
| `stripe.ts` | `OPTIONS /api/stripe/subscription` | ❌ |
| `notifications.ts` | `OPTIONS /api/notifications/dispatch` | ❌ |

**4 of 9 routers pinned.** Each pin is on a different router file,
so a per-router cors override on any single router would fail at
least one probe. The remaining 5 are tomorrow-targets (pick at
least 1 to grow to 5-of-9).

## Open follow-ups — not auto-fixed

Same as 2026-05-08 plus carryovers, with several resolved today:

- **Sandbox corruption on Edit-tool calls — DID NOT RECUR TODAY**.
  Five occurrences in 8 days (2026-05-01 morning, 2026-05-05
  morning, 2026-05-06, 2026-05-07, 2026-05-08), then a clean run
  today. The mitigation (single atomic Python rewrite from the bash
  sandbox) is now the documented workflow. Worth filing an explicit
  infrastructure ticket — same recommendation as 2026-05-06,
  2026-05-07, and 2026-05-08. **Status: dormant — keep watching.**
- **Sandbox `ENOSPC` — seventh day in a row**: the `/sessions` mount
  is still at 100% disk usage, blocking `npx`, `npm install`, and any
  attempt to install or run jest. The TypeScript bundle in
  `node_modules/typescript/lib/typescript.js` is still missing. This
  is an infrastructure issue that no per-day QA run can fix from
  inside the sandbox. **Status: unresolved.**
- The 2026-04-29 `user_activity_events` audit-table migration shipped,
  but nothing in `apps/api/src/` or `apps/web/src/` writes to the
  table or reads from `v_user_activity` yet. Code-side gap, not
  test-side. **Carried since 2026-04-30.**
- `CLAUDE.md` still documents `GET /api/analytics` as an authed
  analytics summary endpoint. The route is intentionally gone (smoke
  pins it as a 404). Doc cleanup deferred. **Carried since 2026-05-01.**
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user —
  only empty-body 400 is probed. Requires DB seed to test, deferred.
  **Carried since 2026-05-03.**
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape (`{ success: false, error: { code:
  'RATE_LIMITED', … } }`). The 429 boundary itself remains unpinned
  because hitting it from a smoke probe would require ~60 sequential
  requests. **Carried from 2026-05-06.**
- `/:id/walkscore`, `/:id/insurance`, `/:id/insurability`,
  `/:id/carriers` Cache-Control (cached) **were resolved 2026-05-08**.
- `/:id/risk?refresh=true` no-cache branch **was resolved 2026-05-08**.
- `/:id/public-data` Cache-Control (24h s-maxage=86400) **was
  resolved today 2026-05-09**.
- `/:id/insurance|carriers|insurability|walkscore?refresh=true`
  no-cache branches **were all resolved today 2026-05-09**.
- `/:id/public-data?refresh=true` no-cache branch remains the only
  unpinned refresh branch. **New tomorrow-target — single probe.**
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires
  an auth-token strategy in the smoke test (the handler is
  `requireAuth`-gated, so an unauth probe gets a 401 from middleware
  before the handler ever calls `setHeader`). **New tomorrow-target
  — needs design: either a long-lived smoke-test JWT in the env, or
  a dedicated `/api/dev/health-token` endpoint behind an env flag.**
- OPTIONS preflight on a fifth mounted surface (`/api/advisor`,
  `/api/deals`, `/api/alerts`, `/api/stripe`, or
  `/api/notifications`) — to grow the per-router pin matrix from
  4-of-9 to 5-of-9 routers. **New tomorrow-target.**

## PR — next step for the user

The sandbox running this scheduled task may or may not have git push
access depending on day. Today's diff is staged on disk; the
follow-up is to push the branch and open the PR. A Chrome browser
tab will be opened to the PR-creation URL for
`main...claude/daily-qa-2026-05-09`, which will render the diff once
the branch is pushed:
`https://github.com/Cover-Guard/Main/compare/main...claude/daily-qa-2026-05-09?quick_pull=1`

To finish the run, push the branch and open the PR:

```powershell
cd "C:\CoverGuard Repo Clone\Main"
git checkout -b claude/daily-qa-2026-05-09
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-09.test.ts `
        daily-qa-2026-05-09.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-09

- No bug fix needed today: smoke-test.ts started clean. The
  recurring Edit-tool corruption (5 occurrences in 8 days) did
  NOT recur. Mitigation: today's edits applied via single
  atomic Python rewrite from the bash sandbox instead of an
  Edit-tool chain.
- Add 6 new probes covering all of yesterday's tomorrow-targets:
  OPTIONS /api/dashboard allowed-origin (4th surface in the
  per-router CORS pin matrix; 4-of-9 routers now covered),
  GET /:id/public-data Cache-Control s-maxage=86400 (24h)
  (closes the penultimate cached-endpoint gap; only report.pdf
  remains, auth-gated),
  GET /:id/insurance?refresh=true no-cache,
  GET /:id/carriers?refresh=true no-cache (VA-01 carrier-exit
  alert SLA),
  GET /:id/insurability?refresh=true no-cache,
  GET /:id/walkscore?refresh=true no-cache (closes the
  refresh=true matrix at 5-of-6).
- Tighten file-integrity guard: line-count floor 1000 -> 1200;
  carry forward 'carriers shou' truncation-marker detection;
  add cross-check that on-disk runTest count >= 86; pin the
  per-router CORS matrix at 4-of-9.
- Document negative pin: /:id/public-data?refresh=true is NOT
  yet pinned (the only remaining refresh branch); a future PR
  adding the probe will update the expectation."
git push -u origin claude/daily-qa-2026-05-09
```

## Suggested PR body

```markdown
## What

Daily smoke + QA pass for 2026-05-09. No bug fix + six new probes +
tightened file-integrity guard.

## Why

**No bug fix this run.** `scripts/qa/smoke-test.ts` started clean
(1085 lines, 80 probes, no truncation, no orphan fragments). The
Edit-tool corruption that bit the file FIVE times in 8 days
(2026-05-01 morning, 2026-05-05 morning, 2026-05-06, 2026-05-07,
2026-05-08) did NOT recur today. To keep it that way, today's 6
new probes were applied via a single atomic Python rewrite (run
from the bash sandbox) instead of a chain of Edit-tool calls. This
is now the documented workflow for any modification to this file.

**Coverage expansion.** Six new probes covering all of yesterday's
tomorrow-targets in one pass:

1. `OPTIONS /api/dashboard` allowed-origin → 204, ACAO echoed
   (fourth mounted surface in the per-router CORS pin matrix;
   4-of-9 routers now covered).
2. `GET /api/properties/:id/public-data` Cache-Control includes
   `s-maxage=86400` (24h CDN cache; closes the penultimate gap in
   the cached-endpoint matrix — only `/:id/report.pdf` remains,
   and it's auth-gated).
3. `GET /api/properties/:id/insurance?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=` (the
   `setNoCacheHeaders` branch on `/insurance`).
4. `GET /api/properties/:id/carriers?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=` (the
   `setNoCacheHeaders` branch on `/carriers`; especially important
   given the VA-01 carrier-exit alert SLA).
5. `GET /api/properties/:id/insurability?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=`.
6. `GET /api/properties/:id/walkscore?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=`. Closes the
   refresh=true matrix on a single `--property-id` run (5 of 6
   refresh branches pinned; only `/:id/public-data?refresh=true`
   remains for tomorrow).

**Tightened guard.** `daily-review-2026-05-09.test.ts` carries
forward the 2026-05-08 file-integrity guard and:

- raises the line-count floor from 1000 to 1200
- carries forward the `carriers shou$` truncation-marker check
- adds a cross-check that on-disk `runTest(` count is at least 86
  (must match the contract list)
- documents the per-router CORS pin matrix (4-of-9 routers now
  covered)
- documents the cached-endpoint Cache-Control matrix (7-of-8 cached
  pinned + 5-of-6 refresh pinned = 12-of-14 cells)
- adds a negative pin: `/:id/public-data?refresh=true` is NOT yet
  pinned (a future PR will update the expectation)

## Files

- `scripts/qa/smoke-test.ts` — atomic Python rewrite, +6 new probes
  (1294 lines, 86 probes).
- `apps/api/src/__tests__/daily-review-2026-05-09.test.ts` — pins
  the 86-entry probe list, tightened file-integrity guard, contract
  docs.

## Testing

Static review only — sandbox can't bring up a live API and `/sessions`
is at 100% disk so `npx` / `npm install` are blocked (seventh day).
Once the dev stack is up:

```bash
npm run qa:smoke -- --property-id <real-uuid>
npm test --workspace apps/api
```
```

## Tomorrow's targets

- `/:id/public-data?refresh=true` Cache-Control no-cache branch.
  Last unpinned refresh branch — closes the refresh=true matrix at
  6-of-6.
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires
  an auth-token strategy. Either a long-lived smoke-test JWT in the
  env, or a dedicated `/api/dev/health-token` endpoint behind an
  env flag. Closes the cached-endpoint matrix at 8-of-8.
- OPTIONS preflight on a fifth mounted surface — `/api/advisor`,
  `/api/deals`, `/api/alerts`, `/api/stripe`, or
  `/api/notifications`. Grows the per-router CORS pin matrix from
  4-of-9 to 5-of-9.
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape. Carried from 2026-05-06.
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user
  (carried since 2026-05-03 — needs DB seed).
- File an infrastructure ticket for: (a) the recurring Edit-tool
  corruption around 30 KB (dormant today, but mitigation lives
  outside the test code), and (b) the `/sessions` 100% disk usage
  that's been blocking jest for seven days running. Both are
  multi-day patterns that no single per-day QA run can resolve.
- Once the cached-endpoint matrix is fully covered (after
  `/:id/report.pdf` lands), the next axis is response-shape pinning
  (e.g. that the `data` object on `/report` has the documented 5
  fields and not 4 or 6).
