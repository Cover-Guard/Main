# Daily smoke + QA run — 2026-05-10

Scheduled task: `daily-smokeqa-testing`. Today's run had no bug fixes —
the smoke-test.ts file started clean for the **second consecutive day**
(1294 lines from yesterday's restored state, 86 probes intact, no
truncation marker, no orphan fragments, brace balance 419/419). The
recurring Edit-tool corruption that bit the file FIVE times in 8 days
did NOT recur today either. The whole run was coverage expansion: a
2-probe pass that closes the **last unpinned refresh=true branch**
(`/:id/public-data?refresh=true`, closing the refresh matrix at 6-of-6)
and grows the **per-router CORS pin matrix to 5-of-9 routers**
(`OPTIONS /api/advisor/chat`, fifth distinct router file).

## What ran

| Suite | Status | Notes |
|---|---|---|
| `npx tsx scripts/qa/smoke-test.ts` | **fail-to-fetch** | Same sandbox constraint as 2026-05-02 / 03 / 04 / 05 / 06 / 07 / 08 / 09 — no live API on :4000. Plus today, `npx` itself returned `ENOSPC` again (sandbox `/sessions` is at 100% disk usage — eighth day in a row). The TypeScript bundle in `node_modules/typescript/lib/typescript.js` is missing as well; `node_modules/.bin/tsx` and `node_modules/.bin/jest` are also absent. Static structural review only. |
| Static structural review of `smoke-test.ts` | **passed (no fix needed)** | Initial state on disk: 1294 lines, ends with `})` after `run().catch(...)` block, 86 `await runTest(` invocations, no `carriers shou$` truncation marker, no `verage` orphan at column 1 (only references in changelog comments), balanced braces (419 open / 419 close). The file was preserved cleanly from 2026-05-09. |
| `npx tsx scripts/qa/health-check.ts` | not executed | no live API in sandbox |
| `npx tsx scripts/qa/env-validate.ts` | not executed | no `.env` in sandbox |
| `apps/api` jest suite | not executed | `node_modules/.bin/jest` shim missing in mount; `npx jest` blocked by `ENOSPC`; same constraint as prior runs. |

## Bugs fixed (0)

The recurring Edit-tool corruption did **not** recur today. Two
consecutive clean days. The mitigation established 2026-05-09
(single atomic Python rewrite from the bash sandbox) was reused
today and again worked cleanly. Today's edits were applied via
`python3 /tmp/patch_smoke_05_10_v2.py` — three sequential
`str.replace` operations (header changelog + advisor OPTIONS probe +
public-data?refresh=true probe) followed by one atomic
`PATH.write_text(...)`. Pre- and post-write integrity checks (line
count, probe count, run().catch() count, brace balance,
truncation-marker absence) all passed.

This sidesteps the recurring `~30 KB sequential-Edit truncation` bug
entirely, and remains the documented workflow for any modification to
`smoke-test.ts` going forward.

Static review of all nine routers (advisor, alerts, auth, clients,
dashboard, deals, notifications, properties, stripe) found no missing
`requireAuth` and no mismatched verb pairs. CORS configuration in
`apps/api/src/index.ts` remains correct. The cached-endpoint Cache-
Control matrix is unchanged at **7 of 8 endpoints** (only
`/:id/report.pdf` remains, and it's auth-gated). The refresh=true
matrix is now **CLOSED at 6-of-6**.

## Coverage gaps closed (2 new probes + tightened file-integrity guard)

`Main/scripts/qa/smoke-test.ts` (1395 lines, 88 probes, +2 from yesterday):

1. **`OPTIONS /api/advisor/chat` from allowed origin → 204, ACAO
   echoed (and not `*`)** — fifth mounted surface in the per-router
   cors pin matrix, beyond `/api/properties/search` (2026-05-06),
   `/api/auth/me` (2026-05-07), `/api/clients` (2026-05-08), and
   `/api/dashboard` (2026-05-09). `advisor.ts` is yet another router
   file, and `/api/advisor/chat` is the AI-assisted insurance advisor
   surface; a per-router cors override on advisor would let a
   malicious origin issue credentialed POSTs to the chat handler from
   a victim's browser and bill the victim's free-tier usage allowance
   against an attacker's prompt — a regression class with both a
   security blast (CSRF-via-CORS into a credentialed POST) and a
   billing blast (usage-quota exhaustion). Note: advisor/chat is
   POST-only, so the preflight `Access-Control-Request-Method`
   header is POST (not GET like the prior four pins). **Per-router
   pin matrix is now 5-of-9 routers.**

2. **`GET /api/properties/:id/public-data?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=`** — pins the
   `setNoCacheHeaders` branch on `/public-data`. **Closes the
   refresh=true matrix at 6-of-6 endpoints — every refresh branch
   exposed by the properties router is now pinned.** public-data is
   the heaviest aggregate (images + tax + listings + amenities), so
   a regression that drops `setNoCacheHeaders` after a force-refresh
   would let the CDN serve a 24-hour stale response after the caller
   explicitly asked for a fresh recompute — the worst-case staleness
   of any refresh branch.

`Main/apps/api/src/__tests__/daily-review-2026-05-10.test.ts` (~580
lines, 7 describe blocks, ~28 it blocks):

3. **Smoke-test surface contract (2026-05-10)** — pins the 88-entry
   `REQUIRED_SMOKE_PROBES_05_10` array (≥88 probes, strictly grows
   over the 86 of 2026-05-09, every entry uniquely worded, today's
   2 additions are each individually asserted). Reverse-pin: every
   `refresh=true` probe (now 6 of them) must NOT assert `s-maxage=`.
4. **Smoke-test file integrity** — carries forward the 2026-05-09
   widened guard. Tightens line-count floor from 1200 to 1300.
   Carries forward the today-specific `carriers shou$` truncation-
   marker check from yesterday. Adds an extra cross-check: at least
   88 `runTest(` invocations on disk (must match the contract list
   above). Adds a structural-corruption guard: balanced braces.
5. **OPTIONS /api/advisor/chat preflight** — documents the per-router
   pin matrix invariant (5 of 9 routers) and asserts 6 OPTIONS-
   prefixed probes total (search disallowed + search allowed + auth/
   me + clients + dashboard + advisor). Documents the four remaining
   router targets (deals, alerts, stripe, notifications) as
   carryovers — pinning them as a *negative* assertion so a future
   PR adding the probe will need to update the expectation.
6. **Property-id-bound Cache-Control contracts** — documents the
   8-endpoint Cache-Control matrix (7 cached pinned, 1 unpinned —
   report.pdf is auth-gated). Documents the 6-endpoint refresh=true
   matrix (6 pinned — **CLOSED**). Asserts ≥7 `s-maxage=N` cache
   pins and exactly 6 refresh=true no-cache pins.
7. **Refresh-branch matrix completeness (closed today)** — asserts
   that the 6 refresh=true probes specifically cover risk + insurance
   + carriers + insurability + walkscore + public-data. The
   completeness check now iterates the 6 cached roots and asserts
   each has a corresponding refresh-branch probe.
8. **Verb-parity audit** — carryover from 2026-05-03. No new
   mutating verbs today (today's additions are an OPTIONS preflight
   + a GET refresh=true). Asserts that every PATCH/DELETE probe in
   the surface contract ends with `→ 401`.
9. **CORS configuration sanity** — documents the per-router pin
   matrix (5-of-9 routers). Asserts ≥6 OPTIONS-prefixed probes and
   confirms each of the five pinned surfaces appears.

## Files touched

- `Main/scripts/qa/smoke-test.ts` — applied 2 new probes via a
  single atomic Python rewrite (run from `/tmp/patch_smoke_05_10_v2.py`).
  Grew from 1294 lines / 86 probes / 65189 bytes to 1395 lines / 88
  probes. Added the 2026-05-10 changelog block at the top, the
  `OPTIONS /api/advisor/chat` always-on probe at line ~649, and the
  property-id-bound `public-data?refresh=true` probe at line ~1190.
  The cached-endpoint matrix is unchanged at 7 of 8 endpoints (only
  `report.pdf` remains, auth-gated). The refresh=true matrix is now
  CLOSED at 6 of 6 endpoints.
- `Main/apps/api/src/__tests__/daily-review-2026-05-10.test.ts` (new)
  — pinned probe list (88 entries), tightened file-integrity guard
  (line-count floor 1200 → 1300, today's runTest cross-check, brace-
  balance guard), per-router CORS pin matrix doc (5-of-9), cached-
  endpoint Cache-Control matrix doc, refresh-branch matrix doc with
  the completeness check (6-of-6 closed).
- `Main/daily-qa-2026-05-10.md` (new) — this report.

## Probe count over time

| Date | Probes | Delta | Notes |
|---|---|---|---|
| 2026-04-30 | ~28 | baseline | search → 401, /health, /robots.txt, suggest, ticker, deals, alerts, vapid |
| 2026-05-01 morning | ~32 | +4 | analytics → 404, advisor 401, geocode 400, walkscore 404 |
| 2026-05-01 evening | ~46 | +14 | full unauth-401 set, register 400, totally-fake 404, carriers happy-path |
| 2026-05-02 | ~47 | +14 (write-side + property-id-bound) | PATCH/DELETE /me, POST clients/deals, ack alert, walkscore 200, public-data, report.pdf 401, checklists, save, quote-request |
| 2026-05-03 | ~53 | +9 | PATCH/DELETE clients/deals, bare detail 404, webhook 400, save DELETE, checklists PATCH/DELETE |
| 2026-05-04 | 56 | +3 | unauth /report 404, /report.pdf 404 (NOT 401), /report 200 |
| 2026-05-05 | 61 | +5 | HEAD /health, GET /properties 404, GET webhook 404, POST search 404, HEAD /report.pdf 401 |
| 2026-05-06 | 68 | +7 | OPTIONS preflight × 2, geocode boundaries × 2, HEAD bogus /report.pdf 404 |
| 2026-05-07 | 74 | +6 | suggest CC, suggest standard rate-limit headers, OPTIONS /api/auth/me, /:id CC, /:id/risk CC |
| 2026-05-08 | 80 | +6 | OPTIONS /api/clients, walkscore CC, insurance CC, insurability CC, carriers CC (1h NOT 2h), risk?refresh=true no-cache |
| 2026-05-09 | 86 | +6 | OPTIONS /api/dashboard, public-data CC, insurance?refresh=true, carriers?refresh=true, insurability?refresh=true, walkscore?refresh=true |
| 2026-05-10 | 88 | +2 | OPTIONS /api/advisor/chat (5th-router CORS pin), public-data?refresh=true (closes refresh matrix) |

## Cached-endpoint Cache-Control matrix (status as of 2026-05-10)

| Endpoint | TTL (s-maxage) | Cached pinned? | Refresh branch pinned? |
|---|---|---|---|
| `GET /api/properties/suggest` | 300 (5 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id` | 1800 (30 min) | ✅ since 2026-05-07 | n/a (no refresh branch) |
| `GET /api/properties/:id/risk` | 7200 (2 h) | ✅ since 2026-05-07 | ✅ since 2026-05-08 |
| `GET /api/properties/:id/insurance` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/insurability` | 7200 (2 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/carriers` | 3600 (1 h, NOT 2h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/walkscore` | 86400 (24 h) | ✅ since 2026-05-08 | ✅ since 2026-05-09 |
| `GET /api/properties/:id/public-data` | 86400 (24 h) | ✅ since 2026-05-09 | ✅ since 2026-05-10 |
| `GET /api/properties/:id/report.pdf` | private, max-age=300 | ❌ requires auth-token strategy | n/a (auth-gated, never CDN-cached) |

Cached-side: **7 of 8 endpoints pinned** (unchanged). Refresh-side:
**6 of 6 endpoints pinned (CLOSED)**. Total Cache-Control matrix: **13
of 14 cells pinned (93%)**.

## Per-router CORS pin matrix (status as of 2026-05-10)

| Router file | Mounted surface | Pinned by smoke probe? |
|---|---|---|
| `properties.ts` | `OPTIONS /api/properties/search` | ✅ since 2026-05-06 (allowed + denied) |
| `auth.ts` | `OPTIONS /api/auth/me` | ✅ since 2026-05-07 |
| `clients.ts` | `OPTIONS /api/clients` | ✅ since 2026-05-08 |
| `dashboard.ts` | `OPTIONS /api/dashboard/ticker` | ✅ since 2026-05-09 |
| `advisor.ts` | `OPTIONS /api/advisor/chat` | ✅ since 2026-05-10 |
| `deals.ts` | `OPTIONS /api/deals` | ❌ tomorrow's target |
| `alerts.ts` | `OPTIONS /api/alerts/carrier-exits` | ❌ tomorrow's target |
| `stripe.ts` | `OPTIONS /api/stripe/subscription` | ❌ |
| `notifications.ts` | `OPTIONS /api/notifications/dispatch` | ❌ |

**5 of 9 routers pinned.** Each pin is on a different router file,
so a per-router cors override on any single pinned router would fail
at least one probe. The remaining 4 are tomorrow-targets (pick at
least 1 to grow to 6-of-9).

## Open follow-ups — not auto-fixed

Same as 2026-05-09 plus carryovers, with several resolved today:

- **Sandbox corruption on Edit-tool calls — DID NOT RECUR TODAY**.
  Five occurrences in 8 days (2026-05-01 morning, 2026-05-05
  morning, 2026-05-06, 2026-05-07, 2026-05-08), then two
  consecutive clean runs (2026-05-09, 2026-05-10). The mitigation
  (single atomic Python rewrite from the bash sandbox) is now
  standard. Worth filing an explicit infrastructure ticket — same
  recommendation as 2026-05-06 onwards. **Status: dormant — keep
  watching, two-day clean streak.**
- **Sandbox `ENOSPC` — eighth day in a row**: the `/sessions` mount
  is still at 100% disk usage, blocking `npx`, `npm install`, and any
  attempt to install or run jest. The TypeScript bundle in
  `node_modules/typescript/lib/typescript.js` is still missing.
  `node_modules/.bin/tsx` and `node_modules/.bin/jest` are also
  absent. This is an infrastructure issue that no per-day QA run
  can fix from inside the sandbox. **Status: unresolved.**
- The 2026-04-29 `user_activity_events` audit-table migration shipped,
  but nothing in `apps/api/src/` or `apps/web/src/` writes to the
  table or reads from `v_user_activity` yet. Code-side gap, not
  test-side. **Carried since 2026-04-30.**
- `CLAUDE.md` still documents `GET /api/analytics` as an authed
  analytics summary endpoint. The route is intentionally gone (smoke
  pins it as a 404). Doc cleanup deferred. **Carried since 2026-05-01.**
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user —
  only empty-body 400 is probed. Requires DB seed to test, deferred.
  **Carried since 2026-05-03.**
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape (`{ success: false, error: { code:
  'RATE_LIMITED', … } }`). The 429 boundary itself remains unpinned
  because hitting it from a smoke probe would require ~60 sequential
  requests. **Carried from 2026-05-06.**
- `/:id/public-data?refresh=true` no-cache branch **was resolved
  today 2026-05-10** — the refresh=true matrix is now CLOSED.
- `/:id/report.pdf` Cache-Control (private, max-age=300) requires
  an auth-token strategy in the smoke test (the handler is
  `requireAuth`-gated, so an unauth probe gets a 401 from middleware
  before the handler ever calls `setHeader`). **Carryover —
  needs design: either a long-lived smoke-test JWT in the env, or
  a dedicated `/api/dev/health-token` endpoint behind an env flag.**
- OPTIONS preflight on a sixth mounted surface (`/api/deals`,
  `/api/alerts`, `/api/stripe`, or `/api/notifications`) — to grow
  the per-router pin matrix from 5-of-9 to 6-of-9 routers. **New
  tomorrow-target.**

## PR — next step for the user

The sandbox running this scheduled task may or may not have git push
access depending on day. Today's diff is staged on disk; the
follow-up is to push the branch and open the PR. A Chrome browser
tab will be opened to the PR-creation URL for
`main...claude/daily-qa-2026-05-10`, which will render the diff once
the branch is pushed:
`https://github.com/Cover-Guard/Main/compare/main...claude/daily-qa-2026-05-10?quick_pull=1`

Note: yesterday's branch (`claude/daily-qa-2026-05-09`) appears to
still be checked out locally with uncommitted changes in the index.
Today's PR layers on top — when the user pushes, they should choose
either:
  (a) a fresh branch from main with both days' commits squashed
      (cleaner history), or
  (b) a stacked PR — push 2026-05-09 first, then 2026-05-10 onto it
      (preserves the day-by-day audit trail).

To finish the run with option (b):

```powershell
cd "C:\CoverGuard Repo Clone\Main"

# Commit yesterday's pending changes on its own branch first.
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-09.test.ts `
        daily-qa-2026-05-09.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-09"
git push -u origin claude/daily-qa-2026-05-09

# Branch off it for today's pass.
git checkout -b claude/daily-qa-2026-05-10
git add scripts/qa/smoke-test.ts `
        apps/api/src/__tests__/daily-review-2026-05-10.test.ts `
        daily-qa-2026-05-10.md
git commit -m "chore(qa): daily smoke + QA suite for 2026-05-10

- No bug fix needed today: smoke-test.ts started clean for the
  second consecutive day. The recurring Edit-tool corruption (5
  occurrences in 8 days) did NOT recur. Mitigation reused: today's
  edits applied via single atomic Python rewrite from the bash
  sandbox.
- Add 2 new probes covering all of yesterday's tomorrow-targets:
  OPTIONS /api/advisor/chat allowed-origin (5th surface in the
  per-router CORS pin matrix; 5-of-9 routers now covered),
  GET /:id/public-data?refresh=true no-cache (closes the
  refresh=true matrix at 6-of-6 endpoints — every refresh branch
  is now pinned).
- Tighten file-integrity guard: line-count floor 1200 -> 1300;
  add structural balanced-braces guard; carry forward
  'carriers shou' truncation-marker detection; cross-check that
  on-disk runTest count >= 88; pin the per-router CORS matrix at
  5-of-9.
- Document carryover: 4 router files (deals, alerts, stripe,
  notifications) still unpinned on the CORS matrix. report.pdf
  Cache-Control still unpinned (auth-gated, needs token
  strategy)."
git push -u origin claude/daily-qa-2026-05-10
```

## Suggested PR body

```markdown
## What

Daily smoke + QA pass for 2026-05-10. No bug fix + two new probes +
tightened file-integrity guard. **Refresh=true Cache-Control matrix
closed at 6-of-6.**

## Why

**No bug fix this run.** `scripts/qa/smoke-test.ts` started clean
for the second consecutive day (1294 lines, 86 probes, no
truncation, no orphan fragments, brace balance 419/419). The Edit-
tool corruption that bit the file FIVE times in 8 days (2026-05-01
morning, 2026-05-05 morning, 2026-05-06, 2026-05-07, 2026-05-08)
did NOT recur today either. The mitigation established 2026-05-09
(single atomic Python rewrite from the bash sandbox) was reused
today and worked cleanly again.

**Coverage expansion.** Two new probes covering all of yesterday's
tomorrow-targets in one pass:

1. `OPTIONS /api/advisor/chat` allowed-origin → 204, ACAO echoed
   (fifth mounted surface in the per-router CORS pin matrix;
   5-of-9 routers now covered). advisor/chat is POST-only, so
   the preflight Access-Control-Request-Method header is POST.
2. `GET /api/properties/:id/public-data?refresh=true` Cache-Control
   includes `no-store` AND `private`, NOT `s-maxage=` (closes the
   refresh=true matrix at 6-of-6 — every refresh branch exposed
   by the properties router is now pinned).

**Tightened guard.** `daily-review-2026-05-10.test.ts` carries
forward the 2026-05-09 file-integrity guard and:

- raises the line-count floor from 1200 to 1300
- adds a structural balanced-braces guard
- carries forward the `carriers shou$` truncation-marker check
- adds a cross-check that on-disk `runTest(` count is at least 88
- documents the per-router CORS pin matrix (5-of-9 routers now
  covered)
- documents the cached-endpoint Cache-Control matrix (7-of-8 cached
  pinned + 6-of-6 refresh pinned = 13-of-14 cells, 93%)
- adds negative pins on the four remaining router files (deals,
  alerts, stripe, notifications)

## Files

- `scripts/qa/smoke-test.ts` — atomic Python rewrite, +2 new probes
  (1395 lines, 88 probes).
- `apps/api/src/__tests__/daily-review-2026-05-10.test.ts` — pins
  the 88-entry probe list, tightened file-integrity guard, contract
  docs.

## Testing

Static review only — sandbox can't bring up a live API and `/sessions`
is at 100% disk so `npx` / `npm install` are blocked (eighth day).
Once the dev stack is up:

```bash
npm run qa:smoke -- --property-id <real-uuid>
npm test --workspace apps/api
```
```

## Tomorrow's targets

- `/:id/report.pdf` Cache-Control (private, max-age=300) requires
  an auth-token strategy. Either a long-lived smoke-test JWT in the
  env, or a dedicated `/api/dev/health-token` endpoint behind an
  env flag. Closes the cached-endpoint matrix at 8-of-8.
- OPTIONS preflight on a sixth mounted surface — `/api/deals`,
  `/api/alerts`, `/api/stripe`, or `/api/notifications`. Grows the
  per-router CORS pin matrix from 5-of-9 to 6-of-9.
- Rate-limiter 429 boundary on `searchLimiter` (60/min) and the
  rate-limiter response shape. Carried from 2026-05-06.
- `/api/auth/register` 409 DUPLICATE_EMAIL on a real existing user
  (carried since 2026-05-03 — needs DB seed).
- File an infrastructure ticket for: (a) the recurring Edit-tool
  corruption around 30 KB (dormant for two consecutive days now,
  but mitigation lives outside the test code), and (b) the
  `/sessions` 100% disk usage that's been blocking jest for eight
  days running. Both are multi-day patterns that no single per-day
  QA run can resolve.
- Now that the refresh=true matrix is CLOSED, the next axis on the
  cache contract is **response-shape pinning** (e.g. that the `data`
  object on `/report` has the documented 5 fields and not 4 or 6).
- Once the cached-endpoint matrix is fully covered (after
  `/:id/report.pdf` lands), the next axis is **handler-internals
  pinning** — assert that a `?refresh=true` call followed
  immediately by a default call returns a fresh response, not the
  cached one (exercises the server-side cache invalidation, not just
  the response header).
